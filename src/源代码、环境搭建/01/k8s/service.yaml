apiVersion: v1
kind: Service
metadata:
  name: cloud-native-demo
  namespace: cloud-native-demo
  labels:
    app: cloud-native-demo
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: cloud-native-demo
